import { Convert24hrPipe } from './convert24hr.pipe';

describe('Convert24hrPipe', () => {
  it('create an instance', () => {
    const pipe = new Convert24hrPipe();
    expect(pipe).toBeTruthy();
  });
});
